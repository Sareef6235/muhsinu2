rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // TEMPORARY: Fully open for debugging. Revert after fixing connectivity.
      allow read, write: if true;
    }
  }
}
